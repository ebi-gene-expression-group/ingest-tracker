name: atlas-ingest
channels:
  - defaults
dependencies:
  - ncurses=6.1=h0a44026_1
  - openssl
  - pip
  - python
  - sqlite
  - pip:
    - google-api-python-client
    - google-auth
    - gspread
    - mysql-connector
    - oauth2client
    - pandas
    - psycopg2
    - requests
    - tqdm